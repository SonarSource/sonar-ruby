pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'sonar-ruby'

include 'sonar-ruby-plugin'
include 'its:plugin'
include 'its:ruling'
include 'jruby-repackaged'
